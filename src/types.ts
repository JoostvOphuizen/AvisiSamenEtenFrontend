export interface User {
    id: number
    name: string
    email: string
    password: string
    picture: string | undefined
  }

export interface Voordeel {
    id: number;
    titel: string;
  }
  