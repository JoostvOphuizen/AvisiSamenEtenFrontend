export interface User {
    id: number
    name: string
    email: string
    password: string
    picture: string
  }

export interface Voordeel {
    id: number;
    titel: string;
  }
  