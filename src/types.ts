export interface User {
    id: string
    naam: string
    email: string
    password: string
    picture: string | undefined
  }

export interface Voordeel {
    id: number;
    titel: string;
  }
  